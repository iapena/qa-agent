# Known Issues — Harness

Quirks conocidos del HARNESS (locators frágiles, timeouts, config).
Se ELIMINAN cuando se resuelven. No van aquí los bugs del producto (esos → GitLab).

Formato por entrada:
- [YYYY-MM-DD] <capa> — <descripción> — <workaround / estado>

<!-- Aún no hay issues registrados. -->
